(1) box selected at random (e.g. different boxes are highlighted in turn and after a few seconds one remains highlighted---that's the selected box), 
(2) prompt prediction of ball color before seeing contents, and then 
(3) revealing the contents?
